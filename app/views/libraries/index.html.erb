<% @libraries.each do |library| %>
	<div>
		<h3><%= library.name %></h3>
		<% if @current_user %>
			<%= button_to "Join", library_users_path(library) %>
			<%= link_to "Show library", library_path(library) %>
		<% end %>
	</div>
	<br>
<% end %>